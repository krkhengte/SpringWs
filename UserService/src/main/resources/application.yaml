server:
  port: 8081
  
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/micreservices
    username: root
    password: root@123
    driver-class-name: com.mysql.jdbc.Driver
    
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dilect: org.hibernate.dialect.MySQL8Dialect
       